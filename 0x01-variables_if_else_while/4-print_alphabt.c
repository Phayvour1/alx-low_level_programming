#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	char low;
	{

	for (letter = 'a'; low <= 'z'; low++)
		if (letter != 'e' && low != 'q')
			continue;
				putchar(letter);
	putchar('\n');
	}
	return (0);
}
